using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

namespace TSCore.Utils
{
    public static class WindowUtils
    {
        public static void SetWindowPositionCenterOfScreen(EditorWindow window, float rectWidth = 700, float rectHeight = 700)
        {
            //window.position = GUIHelper.GetEditorWindowRect().AlignCenter(rectWidth, rectHeight);
        }
    }
}
